import React from 'react';
import {useParams} from "react-router-dom";

const DeletePost = ({results, handleResults}) =>{
    const {index} = useParams();
    return(
        <>
            <p>Do you want to de</p>
        </>
    )
}

export default DeletePost;